import { Router } from 'express';

const router = Router();

// Placeholder - будет реализовано в F5.2
router.get('/', (_req, res) => {
  res.json({ message: 'Offers endpoint - not implemented yet' });
});

export default router;
