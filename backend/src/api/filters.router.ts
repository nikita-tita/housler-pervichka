import { Router } from 'express';

const router = Router();

// Placeholder - будет реализовано в F5.5
router.get('/', (_req, res) => {
  res.json({ message: 'Filters endpoint - not implemented yet' });
});

export default router;
