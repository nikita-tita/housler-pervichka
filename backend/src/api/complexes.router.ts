import { Router } from 'express';

const router = Router();

// Placeholder - будет реализовано в F5.4
router.get('/', (_req, res) => {
  res.json({ message: 'Complexes endpoint - not implemented yet' });
});

export default router;
