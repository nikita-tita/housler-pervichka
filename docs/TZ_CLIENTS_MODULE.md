# ТЗ: Раздел «Клиенты» — CRM-модуль агента

## 1. ОБЗОР

### 1.1 Цель модуля
Централизованное управление воронкой продаж агента: от первичного контакта с клиентом до завершения сделки. Полная CRM-система внутри платформы.

### 1.2 Ключевые сущности

| Сущность | Таблица БД | Статус реализации |
|----------|-----------|-------------------|
| Клиент | `clients` | Существует в schema.sql, НЕ используется |
| Подборка | `selections` | ✅ Реализовано |
| Фиксация цены | `price_locks` | Существует в schema.sql, НЕ используется |
| Бронирование | `bookings` | ✅ Частично (упрощённая модель) |
| Сделка | `deals` | Существует в schema.sql, НЕ используется |
| Срыв | `deal_cancellations` | Существует в schema.sql, НЕ используется |

### 1.3 Роли пользователей
- **agent** — агент/риэлтор (основной пользователь модуля)
- **admin** — администратор (полный доступ)
- **operator** — оператор платформы (обработка заявок)

---

## 2. НАВИГАЦИЯ И URL-СТРУКТУРА

### 2.1 Добавление в Header

```tsx
// frontend/src/components/Header.tsx
// Добавить после "Подборки" для ролей agent/admin:

<Link href="/clients" className="...">
  Клиенты
</Link>
```

### 2.2 URL-структура раздела

```
/clients                    — Главная страница (редирект на /clients/all)
/clients/all               — Все клиенты (карточки)
/clients/funnel            — Воронка продаж (канбан-доска)
/clients/fixations         — Список фиксаций
/clients/bookings          — Список броней
/clients/deals             — Список сделок
/clients/failures          — Список срывов

/clients/new               — Создание клиента
/clients/[id]              — Карточка клиента (детальная)
/clients/[id]/edit         — Редактирование клиента

/clients/fixations/new     — Создание фиксации
/clients/bookings/new      — Создание брони
```

### 2.3 Табы на странице /clients

```
┌─────────┬──────────┬─────────┬────────┬────────┬─────────┐
│   Все   │ Воронка  │ Фиксации│  Брони │ Сделки │  Срывы  │
└─────────┴──────────┴─────────┴────────┴────────┴─────────┘
```

---

## 3. МОДЕЛИ ДАННЫХ

### 3.1 Таблица `clients` (обновление)

Таблица уже существует в `database/schema.sql`, но не используется. Необходимо:

1. **Создать миграцию** `005_clients_crm.sql`:

```sql
-- Добавляем связь клиента с агентом
ALTER TABLE clients ADD COLUMN IF NOT EXISTS agent_id INTEGER REFERENCES users(id);

-- Источник клиента
CREATE TYPE client_source AS ENUM (
    'manual',           -- Ручной ввод
    'selection',        -- Из подборки
    'booking',          -- Из заявки
    'import',           -- Импорт
    'website'           -- С сайта
);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS source client_source DEFAULT 'manual';

-- Статус клиента в воронке
CREATE TYPE client_stage AS ENUM (
    'new',              -- Новый
    'in_progress',      -- В работе
    'fixation',         -- Фиксация цены
    'booking',          -- Бронь
    'deal',             -- Сделка
    'completed',        -- Завершено
    'failed'            -- Сорвано
);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS stage client_stage DEFAULT 'new';

-- Приоритет
CREATE TYPE client_priority AS ENUM ('low', 'medium', 'high', 'urgent');
ALTER TABLE clients ADD COLUMN IF NOT EXISTS priority client_priority DEFAULT 'medium';

-- Дополнительные поля
ALTER TABLE clients ADD COLUMN IF NOT EXISTS telegram VARCHAR(100);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS whatsapp VARCHAR(20);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS comment TEXT;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS budget_min DECIMAL(15, 2);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS budget_max DECIMAL(15, 2);
ALTER TABLE clients ADD COLUMN IF NOT EXISTS desired_rooms INTEGER[];
ALTER TABLE clients ADD COLUMN IF NOT EXISTS desired_districts INTEGER[];
ALTER TABLE clients ADD COLUMN IF NOT EXISTS desired_deadline DATE;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS next_contact_date DATE;
ALTER TABLE clients ADD COLUMN IF NOT EXISTS last_contact_date DATE;

-- Индексы
CREATE INDEX IF NOT EXISTS idx_clients_agent ON clients(agent_id);
CREATE INDEX IF NOT EXISTS idx_clients_stage ON clients(stage);
CREATE INDEX IF NOT EXISTS idx_clients_priority ON clients(priority);
CREATE INDEX IF NOT EXISTS idx_clients_next_contact ON clients(next_contact_date);
```

### 3.2 TypeScript типы

```typescript
// frontend/src/types/clients.ts

export type ClientSource = 'manual' | 'selection' | 'booking' | 'import' | 'website';
export type ClientStage = 'new' | 'in_progress' | 'fixation' | 'booking' | 'deal' | 'completed' | 'failed';
export type ClientPriority = 'low' | 'medium' | 'high' | 'urgent';

export interface Client {
  id: number;
  agent_id: number;

  // Контакты
  name: string | null;
  email: string | null;
  phone: string | null;
  telegram: string | null;
  whatsapp: string | null;

  // CRM
  source: ClientSource;
  stage: ClientStage;
  priority: ClientPriority;
  comment: string | null;

  // Пожелания
  budget_min: number | null;
  budget_max: number | null;
  desired_rooms: number[] | null;
  desired_districts: number[] | null;
  desired_deadline: string | null;

  // Даты
  next_contact_date: string | null;
  last_contact_date: string | null;
  created_at: string;
  updated_at: string;

  // Агрегации (из JOIN)
  selections_count?: number;
  fixations_count?: number;
  bookings_count?: number;
  deals_count?: number;
}

export interface ClientListItem extends Pick<Client,
  'id' | 'name' | 'phone' | 'email' | 'stage' | 'priority' |
  'next_contact_date' | 'created_at'
> {
  selections_count: number;
  active_fixations: number;
  active_bookings: number;
}

export interface ClientDetail extends Client {
  selections: SelectionBrief[];
  fixations: FixationBrief[];
  bookings: BookingBrief[];
  deals: DealBrief[];
  activity_log: ActivityLogItem[];
}
```

### 3.3 Связь с существующими таблицами

```sql
-- Добавляем client_id к selections (связь подборка → клиент)
ALTER TABLE selections ADD COLUMN IF NOT EXISTS client_id INTEGER REFERENCES clients(id);

-- Добавляем client_id к price_locks
ALTER TABLE price_locks ADD COLUMN IF NOT EXISTS client_id INTEGER REFERENCES clients(id);

-- Добавляем client_id к bookings
ALTER TABLE bookings ADD COLUMN IF NOT EXISTS client_id INTEGER REFERENCES clients(id);

-- Добавляем client_id к deals
ALTER TABLE deals ADD COLUMN IF NOT EXISTS client_id INTEGER REFERENCES clients(id);
```

---

## 4. API ENDPOINTS

### 4.1 Клиенты

```
GET    /api/clients                 — Список клиентов агента
POST   /api/clients                 — Создать клиента
GET    /api/clients/:id             — Детали клиента
PATCH  /api/clients/:id             — Обновить клиента
DELETE /api/clients/:id             — Удалить клиента
PATCH  /api/clients/:id/stage       — Изменить этап воронки
GET    /api/clients/:id/activity    — Лог активности
GET    /api/clients/stats           — Статистика по воронке
```

### 4.2 Фиксации цен

```
GET    /api/fixations               — Список фиксаций
POST   /api/fixations               — Создать фиксацию
GET    /api/fixations/:id           — Детали фиксации
PATCH  /api/fixations/:id/status    — Изменить статус
POST   /api/fixations/:id/convert   — Конвертировать в бронь
```

### 4.3 Сделки

```
GET    /api/deals                   — Список сделок
POST   /api/deals                   — Создать сделку (из брони)
GET    /api/deals/:id               — Детали сделки
PATCH  /api/deals/:id/status        — Изменить статус
```

### 4.4 Срывы

```
GET    /api/failures                — Список срывов
POST   /api/failures                — Зарегистрировать срыв
GET    /api/failures/reasons        — Справочник причин
```

---

## 5. BACKEND СЕРВИСЫ

### 5.1 Структура файлов

```
backend/src/
├── services/
│   ├── clients.service.ts         ← NEW
│   ├── fixations.service.ts       ← NEW
│   ├── deals.service.ts           ← NEW
│   ├── failures.service.ts        ← NEW
│   └── ... (existing)
├── api/
│   ├── controllers/
│   │   ├── clients.controller.ts  ← NEW
│   │   ├── fixations.controller.ts← NEW
│   │   ├── deals.controller.ts    ← NEW
│   │   └── failures.controller.ts ← NEW
│   └── routes/
│       ├── clients.routes.ts      ← NEW
│       ├── fixations.routes.ts    ← NEW
│       ├── deals.routes.ts        ← NEW
│       └── failures.routes.ts     ← NEW
└── types/
    └── clients.types.ts           ← NEW
```

### 5.2 ClientsService

```typescript
// backend/src/services/clients.service.ts

export class ClientsService {
  // Список клиентов агента с пагинацией и фильтрами
  async getAgentClients(agentId: number, filters: ClientFilters): Promise<PaginatedResult<ClientListItem>>

  // Создание клиента
  async createClient(agentId: number, data: CreateClientDto): Promise<Client>

  // Получение клиента по ID
  async getClientById(clientId: number, agentId: number): Promise<ClientDetail | null>

  // Обновление клиента
  async updateClient(clientId: number, agentId: number, data: UpdateClientDto): Promise<Client | null>

  // Удаление клиента
  async deleteClient(clientId: number, agentId: number): Promise<boolean>

  // Изменение этапа воронки
  async updateStage(clientId: number, agentId: number, stage: ClientStage): Promise<Client | null>

  // Получение статистики воронки
  async getFunnelStats(agentId: number): Promise<FunnelStats>

  // Лог активности клиента
  async getActivityLog(clientId: number, agentId: number): Promise<ActivityLogItem[]>

  // Автоматическое создание клиента из подборки/брони
  async createFromSelection(selectionId: number): Promise<Client>
  async createFromBooking(bookingId: number): Promise<Client>
}
```

---

## 6. FRONTEND КОМПОНЕНТЫ

### 6.1 Структура папок

```
frontend/src/
├── app/
│   └── clients/
│       ├── page.tsx               — Главная (редирект)
│       ├── layout.tsx             — Layout с табами
│       ├── all/
│       │   └── page.tsx           — Список всех клиентов
│       ├── funnel/
│       │   └── page.tsx           — Канбан воронки
│       ├── fixations/
│       │   ├── page.tsx           — Список фиксаций
│       │   └── new/
│       │       └── page.tsx       — Создание фиксации
│       ├── bookings/
│       │   └── page.tsx           — Список броней
│       ├── deals/
│       │   └── page.tsx           — Список сделок
│       ├── failures/
│       │   └── page.tsx           — Список срывов
│       ├── new/
│       │   └── page.tsx           — Создание клиента
│       └── [id]/
│           ├── page.tsx           — Карточка клиента
│           └── edit/
│               └── page.tsx       — Редактирование
├── components/
│   └── clients/
│       ├── ClientCard.tsx         — Карточка клиента (в списке)
│       ├── ClientForm.tsx         — Форма создания/редактирования
│       ├── ClientDetail.tsx       — Детальная карточка
│       ├── ClientsTable.tsx       — Таблица клиентов
│       ├── ClientsTabs.tsx        — Табы навигации
│       ├── FunnelBoard.tsx        — Канбан-доска воронки
│       ├── FunnelColumn.tsx       — Колонка канбана
│       ├── FixationCard.tsx       — Карточка фиксации
│       ├── FixationForm.tsx       — Форма создания фиксации
│       ├── BookingCard.tsx        — Карточка брони
│       ├── DealCard.tsx           — Карточка сделки
│       ├── FailureCard.tsx        — Карточка срыва
│       ├── ActivityTimeline.tsx   — Таймлайн активности
│       ├── StageSelector.tsx      — Выбор этапа воронки
│       ├── PriorityBadge.tsx      — Бейдж приоритета
│       └── ClientSearchFilters.tsx— Фильтры поиска
└── types/
    └── clients.ts                 — Типы (см. выше)
```

### 6.2 Layout страницы /clients

```tsx
// frontend/src/app/clients/layout.tsx

'use client';

import { usePathname } from 'next/navigation';
import Link from 'next/link';
import { useAuth } from '@/contexts/AuthContext';
import { redirect } from 'next/navigation';

const TABS = [
  { href: '/clients/all', label: 'Все клиенты' },
  { href: '/clients/funnel', label: 'Воронка' },
  { href: '/clients/fixations', label: 'Фиксации' },
  { href: '/clients/bookings', label: 'Брони' },
  { href: '/clients/deals', label: 'Сделки' },
  { href: '/clients/failures', label: 'Срывы' },
];

export default function ClientsLayout({ children }: { children: React.ReactNode }) {
  const { user, isAuthenticated, isLoading } = useAuth();
  const pathname = usePathname();

  if (isLoading) return <LoadingSkeleton />;
  if (!isAuthenticated || (user?.role !== 'agent' && user?.role !== 'admin')) {
    redirect('/login');
  }

  return (
    <div className="section">
      <div className="container">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <h1 className="text-3xl font-semibold">Клиенты</h1>
          <Link href="/clients/new" className="btn btn-primary">
            + Добавить клиента
          </Link>
        </div>

        {/* Tabs */}
        <div className="flex gap-2 mb-8 overflow-x-auto pb-2">
          {TABS.map(tab => (
            <Link
              key={tab.href}
              href={tab.href}
              className={`px-4 py-2 rounded-lg text-sm font-medium whitespace-nowrap transition-colors
                ${pathname === tab.href
                  ? 'bg-[var(--color-text)] text-white'
                  : 'bg-[var(--color-bg-gray)] hover:bg-gray-200'
                }`}
            >
              {tab.label}
            </Link>
          ))}
        </div>

        {/* Content */}
        {children}
      </div>
    </div>
  );
}
```

---

## 7. UI/UX СПЕЦИФИКАЦИЯ

### 7.1 Страница "Все клиенты" (/clients/all)

```
┌────────────────────────────────────────────────────────────────────┐
│  🔍 Поиск: [ФИО, телефон, email____________]  [Фильтры ▼]         │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │ Иван Петров      │  │ Мария Сидорова   │  │ Алексей Козлов   │ │
│  │ +7 911 123-45-67 │  │ +7 921 987-65-43 │  │ +7 999 555-12-34 │ │
│  │ ─────────────────│  │ ─────────────────│  │ ─────────────────│ │
│  │ 📍 Фиксация      │  │ 📍 Бронь         │  │ 📍 Новый         │ │
│  │ 🏠 2 подборки    │  │ 🏠 1 подборка    │  │ 🏠 0 подборок    │ │
│  │ 📅 Связаться: 5.12│  │ 📅 Связаться: 3.12│  │ 📅 —             │ │
│  │ [🔴 Высокий]     │  │ [🟡 Средний]     │  │ [🟢 Низкий]      │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 7.2 Страница "Воронка" (/clients/funnel)

Канбан-доска с drag-and-drop:

```
┌────────┬────────┬────────┬────────┬────────┬────────┐
│ Новые  │В работе│Фиксация│  Бронь │ Сделка │Завершено│
│  (5)   │  (12)  │  (3)   │  (2)   │  (1)   │  (45)  │
├────────┼────────┼────────┼────────┼────────┼────────┤
│┌──────┐│┌──────┐│┌──────┐│┌──────┐│┌──────┐│┌──────┐│
││Клиент││Клиент ││Клиент ││Клиент ││Клиент ││Клиент ││
│└──────┘│└──────┘│└──────┘│└──────┘│└──────┘│└──────┘│
│┌──────┐│┌──────┐│┌──────┐│┌──────┐│        │┌──────┐│
││Клиент││Клиент ││Клиент ││Клиент ││        ││Клиент ││
│└──────┘│└──────┘│└──────┘│└──────┘│        │└──────┘│
│        │┌──────┐│        │        │        │        │
│        ││Клиент││        │        │        │        │
│        │└──────┘│        │        │        │        │
└────────┴────────┴────────┴────────┴────────┴────────┘
```

### 7.3 Карточка клиента (/clients/[id])

```
┌────────────────────────────────────────────────────────────────────┐
│  ← Назад                                    [Редактировать]        │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Иван Петров                              🔴 Высокий приоритет     │
│  ══════════════════════════════════════════════════════════════   │
│                                                                    │
│  📱 +7 911 123-45-67        📧 ivan@mail.ru                       │
│  💬 @ivan_petrov (Telegram)  📱 WhatsApp: +7 911 123-45-67        │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ Этап воронки:  [Новый] [В работе] [Фиксация] [Бронь] [Сделка]│  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ─────────────────────────────────────────────────────────────────│
│  💰 Бюджет: 8 000 000 — 12 000 000 ₽                              │
│  🏠 Комнаты: 2-3 комнаты                                          │
│  📍 Районы: Приморский, Выборгский                                │
│  📅 Срок: до Q2 2025                                              │
│  ─────────────────────────────────────────────────────────────────│
│                                                                    │
│  📝 Комментарий:                                                  │
│  Ищет квартиру для семьи с ребёнком. Важна близость к метро.     │
│  ─────────────────────────────────────────────────────────────────│
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  📁 Подборки (2)  │  📌 Фиксации (1)  │  📋 Брони (0)        │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │  • Квартиры в Приморском — 5 объектов — Открыть              │ │
│  │  • Варианты до 10 млн — 3 объекта — Открыть                  │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  ─────────────────────────────────────────────────────────────────│
│  📊 История активности:                                           │
│  ─────────────────────────────────────────────────────────────────│
│  01.12.2024 15:30  Создана фиксация на ЖК "Северная долина"      │
│  30.11.2024 12:00  Просмотрел подборку (5 мин)                   │
│  29.11.2024 18:45  Клиент создан из заявки на бронь              │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 7.4 Страница "Фиксации" (/clients/fixations)

```
┌────────────────────────────────────────────────────────────────────┐
│  Фиксации цен                              [+ Создать фиксацию]   │
├────────────────────────────────────────────────────────────────────┤
│  🔍 Поиск  [Статус ▼: Все]  [Дата от ▼]  [Дата до ▼]             │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ PL-2024-0042                              ⏳ Ожидает         │  │
│  │ ───────────────────────────────────────────────────────────  │  │
│  │ Клиент: Иван Петров • +7 911 123-45-67                      │  │
│  │ ЖК: Северная долина • Корпус 3 • кв. 125                    │  │
│  │ Цена: 9 500 000 ₽ • Срок: 7 дней (до 08.12.2024)            │  │
│  │ ───────────────────────────────────────────────────────────  │  │
│  │ [Одобрить] [Отклонить] [→ В бронь]                          │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ PL-2024-0041                              ✅ Одобрено        │  │
│  │ ───────────────────────────────────────────────────────────  │  │
│  │ Клиент: Мария Сидорова • +7 921 987-65-43                   │  │
│  │ ЖК: Галактика • Корпус 1 • кв. 78                           │  │
│  │ Цена: 7 200 000 ₽ • Истекает: 05.12.2024 (2 дня)            │  │
│  │ ───────────────────────────────────────────────────────────  │  │
│  │ [→ В бронь] [Продлить]                                       │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 7.5 Форма создания фиксации (/clients/fixations/new)

```
┌────────────────────────────────────────────────────────────────────┐
│  Создание фиксации цены                                           │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Клиент *                                                         │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ 🔍 Выберите клиента или создайте нового...                 │   │
│  └────────────────────────────────────────────────────────────┘   │
│  [+ Создать нового клиента]                                       │
│                                                                    │
│  ─────────────────────────────────────────────────────────────────│
│                                                                    │
│  Объект недвижимости *                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ 🔍 Поиск по ЖК, адресу или ID...                           │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  [Выбранный объект отображается здесь с ценой и параметрами]     │
│                                                                    │
│  ─────────────────────────────────────────────────────────────────│
│                                                                    │
│  Срок фиксации *                                                  │
│  ┌─────────────┐                                                  │
│  │ 7 дней    ▼ │  (3 / 5 / 7 / 10 / 14 дней)                     │
│  └─────────────┘                                                  │
│                                                                    │
│  Комментарий для застройщика                                      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                                                            │   │
│  │                                                            │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ─────────────────────────────────────────────────────────────────│
│                                                                    │
│              [Отмена]                    [Создать фиксацию]       │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 8. ВОРОНКА ПРОДАЖ — БИЗНЕС-ЛОГИКА

### 8.1 Этапы воронки

```
┌─────────┐    ┌──────────┐    ┌──────────┐    ┌────────┐    ┌────────┐    ┌──────────┐
│  НОВЫЙ  │ →  │ В РАБОТЕ │ →  │ ФИКСАЦИЯ │ →  │ БРОНЬ  │ →  │ СДЕЛКА │ →  │ ЗАВЕРШЕНО│
└─────────┘    └──────────┘    └──────────┘    └────────┘    └────────┘    └──────────┘
     │              │              │              │              │
     └──────────────┴──────────────┴──────────────┴──────────────┴──────→ СРЫВ
```

### 8.2 Переходы между этапами

| Из этапа | В этап | Условие | Действие |
|----------|--------|---------|----------|
| Новый | В работе | Ручной перевод | — |
| В работе | Фиксация | Создание фиксации | Автоматически |
| Фиксация | Бронь | Создание брони | Автоматически |
| Фиксация | В работе | Фиксация истекла/отклонена | Автоматически |
| Бронь | Сделка | Создание сделки | Автоматически |
| Бронь | В работе | Бронь отменена | Автоматически |
| Сделка | Завершено | ДДУ подписан + зарегистрирован | Автоматически |
| Любой | Срыв | Регистрация срыва | Ручной + причина |

### 8.3 Автоматические действия

1. **При создании клиента из подборки:**
   - Создаётся клиент со stage = 'new'
   - Подборка привязывается к клиенту

2. **При создании фиксации:**
   - Если клиент на этапе ниже 'fixation' → перевести в 'fixation'
   - Записать в activity_log

3. **При истечении фиксации:**
   - Если нет активных броней → вернуть в 'in_progress'
   - Отправить уведомление агенту

4. **При создании брони:**
   - Перевести клиента в stage = 'booking'
   - Если была активная фиксация → отметить как 'converted'

5. **При завершении сделки:**
   - Перевести клиента в stage = 'completed'
   - Рассчитать комиссию

---

## 9. ИНТЕГРАЦИЯ С СУЩЕСТВУЮЩИМ КОДОМ

### 9.1 Изменения в SelectionsService

```typescript
// backend/src/services/selections.service.ts

// Добавить метод:
async linkToClient(selectionId: number, clientId: number, agentId: number): Promise<boolean> {
  // Проверка прав
  // UPDATE selections SET client_id = $1 WHERE id = $2 AND agent_id = $3
}

// Изменить createSelection:
async createSelection(agentId: number, data: {
  name: string;
  clientId?: number;        // ← NEW: опциональная привязка к клиенту
  clientEmail?: string;
  clientName?: string;
  isPublic?: boolean;
}): Promise<Selection>
```

### 9.2 Изменения в BookingsService

```typescript
// backend/src/services/bookings.service.ts

// Добавить поле client_id:
async createBooking(data: {
  offerId: number;
  agentId?: number;
  clientId?: number;        // ← NEW
  clientName: string;
  clientPhone: string;
  clientEmail?: string;
  comment?: string;
}): Promise<Booking | null>

// Добавить метод:
async linkToClient(bookingId: number, clientId: number): Promise<boolean>
```

### 9.3 Добавление в index.ts

```typescript
// backend/src/index.ts

import clientsRoutes from './api/routes/clients.routes';
import fixationsRoutes from './api/routes/fixations.routes';
import dealsRoutes from './api/routes/deals.routes';
import failuresRoutes from './api/routes/failures.routes';

// ...

app.use('/api/clients', clientsRoutes);
app.use('/api/fixations', fixationsRoutes);
app.use('/api/deals', dealsRoutes);
app.use('/api/failures', failuresRoutes);
```

### 9.4 Добавление в frontend api.ts

```typescript
// frontend/src/services/api.ts

// ============ CLIENTS ============
async getClients(filters?: ClientFilters): Promise<ApiResponse<PaginatedResponse<ClientListItem>>>
async createClient(data: CreateClientDto): Promise<ApiResponse<Client>>
async getClient(id: number): Promise<ApiResponse<ClientDetail>>
async updateClient(id: number, data: UpdateClientDto): Promise<ApiResponse<Client>>
async deleteClient(id: number): Promise<ApiResponse<void>>
async updateClientStage(id: number, stage: ClientStage): Promise<ApiResponse<Client>>
async getClientActivity(id: number): Promise<ApiResponse<ActivityLogItem[]>>
async getClientStats(): Promise<ApiResponse<FunnelStats>>

// ============ FIXATIONS ============
async getFixations(filters?: FixationFilters): Promise<ApiResponse<PaginatedResponse<Fixation>>>
async createFixation(data: CreateFixationDto): Promise<ApiResponse<Fixation>>
async getFixation(id: number): Promise<ApiResponse<FixationDetail>>
async updateFixationStatus(id: number, status: FixationStatus): Promise<ApiResponse<Fixation>>
async convertFixationToBooking(id: number): Promise<ApiResponse<Booking>>

// ============ DEALS ============
async getDeals(filters?: DealFilters): Promise<ApiResponse<PaginatedResponse<Deal>>>
async createDeal(bookingId: number): Promise<ApiResponse<Deal>>
async getDeal(id: number): Promise<ApiResponse<DealDetail>>
async updateDealStatus(id: number, status: DealStatus): Promise<ApiResponse<Deal>>

// ============ FAILURES ============
async getFailures(filters?: FailureFilters): Promise<ApiResponse<PaginatedResponse<Failure>>>
async createFailure(data: CreateFailureDto): Promise<ApiResponse<Failure>>
async getFailureReasons(): Promise<ApiResponse<FailureReason[]>>
```

---

## 10. ПЛАН РЕАЛИЗАЦИИ

### Этап 1: База данных (1-2 дня)
- [ ] Создать миграцию `005_clients_crm.sql`
- [ ] Обновить связи между таблицами
- [ ] Добавить индексы

### Этап 2: Backend — Клиенты (2-3 дня)
- [ ] `ClientsService` с базовым CRUD
- [ ] `ClientsController`
- [ ] `clients.routes.ts`
- [ ] Тесты

### Этап 3: Backend — Фиксации, Сделки, Срывы (2-3 дня)
- [ ] `FixationsService`
- [ ] `DealsService`
- [ ] `FailuresService`
- [ ] Контроллеры и роуты

### Этап 4: Frontend — Layout и навигация (1 день)
- [ ] Добавить "Клиенты" в Header
- [ ] Layout с табами
- [ ] Редирект /clients → /clients/all

### Этап 5: Frontend — Список клиентов (2-3 дня)
- [ ] Страница /clients/all
- [ ] Компоненты ClientCard, ClientsTable
- [ ] Фильтры и поиск

### Этап 6: Frontend — Воронка (2-3 дня)
- [ ] Страница /clients/funnel
- [ ] FunnelBoard с drag-and-drop
- [ ] Обновление этапа через API

### Этап 7: Frontend — Карточка клиента (2-3 дня)
- [ ] Страница /clients/[id]
- [ ] ClientDetail с табами
- [ ] ActivityTimeline

### Этап 8: Frontend — Фиксации (2-3 дня)
- [ ] Страница /clients/fixations
- [ ] Форма создания фиксации
- [ ] Конвертация в бронь

### Этап 9: Frontend — Сделки и срывы (1-2 дня)
- [ ] Страницы /clients/deals и /clients/failures
- [ ] Формы и карточки

### Этап 10: Интеграция и тестирование (2-3 дня)
- [ ] Связь подборок с клиентами
- [ ] Связь броней с клиентами
- [ ] Автоматические переходы воронки
- [ ] E2E тестирование

---

## 11. ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ (BACKLOG)

### 11.1 Уведомления
- Email при изменении статуса фиксации
- Push-уведомления об истечении фиксаций
- Напоминания о запланированных контактах

### 11.2 Аналитика
- Дашборд с метриками воронки
- Конверсия между этапами
- Средний срок на каждом этапе

### 11.3 Импорт/Экспорт
- Импорт клиентов из Excel
- Экспорт отчётов

### 11.4 Интеграции
- Интеграция с CRM (AmoCRM, Bitrix24)
- Telegram-бот для уведомлений

---

## 12. ПРИМЕЧАНИЯ

### 12.1 Отличия от TrendAgent
- У нас более простая структура без разделения на "бесплатные/платные брони"
- Нет отдельного раздела "Просмотры" — это часть аналитики подборок
- Фиксации и сделки интегрированы в единую воронку

### 12.2 Совместимость
- Все изменения обратно совместимы с текущим функционалом
- Существующие `selections` и `bookings` продолжат работать
- Новый функционал опционален — можно не создавать клиентов

### 12.3 Миграция данных
- При необходимости можно создать скрипт для автоматического создания клиентов из существующих `bookings`
